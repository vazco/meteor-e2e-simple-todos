{
  "name": "simple-todos",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "e2e": "meteor test --full-app --driver-package universe:e2e --raw-logs",
    "test": "CI=1 meteor test --full-app --driver-package universe:e2e --raw-logs --once"
  },
  "dependencies": {
    "@babel/runtime": "7.0.0-beta.46",
    "bcrypt": "1.0.3",
    "classnames": "2.2.5",
    "meteor-node-stubs": "0.3.3",
    "react": "16.2.0",
    "react-dom": "16.2.0"
  },
  "devDependencies": {
    "chai": "4.1.2",
    "faker": "4.1.0",
    "mocha": "5.1.1",
    "puppeteer": "1.3.0"
  }
}
